import { LookupEntity } from './../lookupEntity';

export class TagAdmin extends LookupEntity {
    tagName: string='';
    tagPrefix:  string='';
    custom: string='';
    organizationId: string='';
}